databaseChangeLog:
  - preConditions:
      - runningAs:
          username: root

  - changeSet:
      id: create-portal-schema-1
      author: gavinkillough
      changes:
        - sql: create schema if not exists portal;